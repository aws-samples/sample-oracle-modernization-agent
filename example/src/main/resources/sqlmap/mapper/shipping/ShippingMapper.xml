<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="amzn.bo.mapper.ShippingMapper">

    <!-- 배송 성과 분석 - 실제 테이블 구조에 맞게 수정 -->
    <select id="selectShippingPerformanceAnalysis" parameterType="map" resultType="map">
        SELECT 
            s.SHIPPING_METHOD,
            s.SHIPPING_CARRIER as CARRIER,
            COUNT(DISTINCT s.SHIPMENT_ID) as TOTAL_SHIPMENTS,
            COUNT(CASE WHEN s.SHIPMENT_STATUS = 'DELIVERED' THEN 1 END) as DELIVERED_COUNT,
            COUNT(CASE WHEN s.SHIPMENT_STATUS = 'IN_TRANSIT' THEN 1 END) as IN_TRANSIT_COUNT,
            COUNT(CASE WHEN s.SHIPMENT_STATUS = 'DELAYED' THEN 1 END) as DELAYED_COUNT,
            ROUND(COUNT(CASE WHEN s.SHIPMENT_STATUS = 'DELIVERED' THEN 1 END) * 100.0 / COUNT(*), 2) as DELIVERY_SUCCESS_RATE,
            ROUND(AVG(CASE WHEN s.ACTUAL_DELIVERY_DATE IS NOT NULL AND s.SHIPPED_DATE IS NOT NULL 
                THEN (CAST(s.ACTUAL_DELIVERY_DATE AS DATE) - CAST(s.SHIPPED_DATE AS DATE)) END), 2) as AVG_DELIVERY_DAYS,
            NVL(SUM(s.SHIPPING_COST), 0) as TOTAL_SHIPPING_COST,
            ROUND(NVL(AVG(s.SHIPPING_COST), 0), 2) as AVG_SHIPPING_COST
        FROM SHIPMENTS s
        WHERE s.CREATED_AT >= SYSDATE - 30
        GROUP BY s.SHIPPING_METHOD, s.SHIPPING_CARRIER
        ORDER BY DELIVERY_SUCCESS_RATE DESC, TOTAL_SHIPMENTS DESC
    </select>

    <!-- 배송 지연 패턴 분석 - 실제 테이블 구조에 맞게 수정 -->
    <select id="selectShippingDelayPatternAnalysis" parameterType="map" resultType="map">
        SELECT 
            delay_reason,
            shipping_method,
            carrier,
            COUNT(*) as DELAY_COUNT,
            ROUND(AVG(delay_days), 2) as AVG_DELAY_DAYS,
            ROUND(MIN(delay_days), 2) as MIN_DELAY_DAYS,
            ROUND(MAX(delay_days), 2) as MAX_DELAY_DAYS,
            COUNT(CASE WHEN EXTRACT(HOUR FROM created_at) BETWEEN 0 AND 5 THEN 1 END) as NIGHT_DELAYS,
            COUNT(CASE WHEN EXTRACT(HOUR FROM created_at) BETWEEN 6 AND 11 THEN 1 END) as MORNING_DELAYS,
            COUNT(CASE WHEN EXTRACT(HOUR FROM created_at) BETWEEN 12 AND 17 THEN 1 END) as AFTERNOON_DELAYS,
            COUNT(CASE WHEN EXTRACT(HOUR FROM created_at) BETWEEN 18 AND 23 THEN 1 END) as EVENING_DELAYS
        FROM (
            SELECT 
                s.SHIPPING_METHOD,
                s.SHIPPING_CARRIER as carrier,
                s.CREATED_AT,
                CASE 
                    WHEN s.SHIPMENT_STATUS = 'DELAYED' THEN 'CARRIER_DELAY'
                    WHEN s.ACTUAL_DELIVERY_DATE IS NOT NULL AND s.ESTIMATED_DELIVERY_DATE IS NOT NULL 
                         AND CAST(s.ACTUAL_DELIVERY_DATE AS DATE) &gt; CAST(s.ESTIMATED_DELIVERY_DATE AS DATE) THEN 'LATE_DELIVERY'
                    WHEN s.SHIPPED_DATE IS NOT NULL AND s.CREATED_AT IS NOT NULL 
                         AND (CAST(s.SHIPPED_DATE AS DATE) - CAST(s.CREATED_AT AS DATE)) &gt; 2 THEN 'PROCESSING_DELAY'
                    ELSE 'OTHER_DELAY'
                END as delay_reason,
                CASE 
                    WHEN s.ACTUAL_DELIVERY_DATE IS NOT NULL AND s.ESTIMATED_DELIVERY_DATE IS NOT NULL 
                    THEN CAST(s.ACTUAL_DELIVERY_DATE AS DATE) - CAST(s.ESTIMATED_DELIVERY_DATE AS DATE)
                    WHEN s.SHIPPED_DATE IS NOT NULL AND s.CREATED_AT IS NOT NULL 
                    THEN CAST(s.SHIPPED_DATE AS DATE) - CAST(s.CREATED_AT AS DATE)
                    ELSE 1
                END as delay_days
            FROM SHIPMENTS s
            WHERE (s.SHIPMENT_STATUS = 'DELAYED' 
                   OR (s.ACTUAL_DELIVERY_DATE IS NOT NULL AND s.ESTIMATED_DELIVERY_DATE IS NOT NULL 
                       AND CAST(s.ACTUAL_DELIVERY_DATE AS DATE) &gt; CAST(s.ESTIMATED_DELIVERY_DATE AS DATE))
                   OR (s.SHIPPED_DATE IS NOT NULL AND s.CREATED_AT IS NOT NULL 
                       AND (CAST(s.SHIPPED_DATE AS DATE) - CAST(s.CREATED_AT AS DATE)) &gt; 2))
            AND s.CREATED_AT >= SYSDATE - 30
        )
        GROUP BY delay_reason, shipping_method, carrier
        ORDER BY DELAY_COUNT DESC
    </select>

    <!-- 배송 경로 최적화 분석 - 단순화된 버전 -->
    <select id="selectShippingRouteOptimizationAnalysis" parameterType="map" resultType="map">
        SELECT 
            'ALL_REGIONS' as origin_region,
            'ALL_DESTINATIONS' as destination_region,
            s.SHIPPING_METHOD,
            COUNT(*) as SHIPMENT_COUNT,
            100 as AVG_DISTANCE_KM,
            ROUND(AVG(CASE WHEN s.ACTUAL_DELIVERY_DATE IS NOT NULL AND s.SHIPPED_DATE IS NOT NULL 
                THEN (CAST(s.ACTUAL_DELIVERY_DATE AS DATE) - CAST(s.SHIPPED_DATE AS DATE)) END), 2) as AVG_DELIVERY_DAYS,
            ROUND(AVG(s.SHIPPING_COST), 2) as AVG_SHIPPING_COST,
            ROUND(AVG(s.SHIPPING_COST / 100), 4) as COST_PER_KM,
            COUNT(CASE WHEN (CASE WHEN s.ACTUAL_DELIVERY_DATE IS NOT NULL AND s.SHIPPED_DATE IS NOT NULL 
                THEN (CAST(s.ACTUAL_DELIVERY_DATE AS DATE) - CAST(s.SHIPPED_DATE AS DATE)) ELSE 999 END) &lt;= 
                (CASE WHEN s.SHIPPING_METHOD = 'EXPRESS' THEN 1 WHEN s.SHIPPING_METHOD = 'STANDARD' THEN 3 ELSE 5 END) THEN 1 END) as ON_TIME_DELIVERIES,
            ROUND(COUNT(CASE WHEN (CASE WHEN s.ACTUAL_DELIVERY_DATE IS NOT NULL AND s.SHIPPED_DATE IS NOT NULL 
                THEN (CAST(s.ACTUAL_DELIVERY_DATE AS DATE) - CAST(s.SHIPPED_DATE AS DATE)) ELSE 999 END) &lt;= 
                (CASE WHEN s.SHIPPING_METHOD = 'EXPRESS' THEN 1 WHEN s.SHIPPING_METHOD = 'STANDARD' THEN 3 ELSE 5 END) THEN 1 END) * 100.0 / COUNT(*), 2) as ON_TIME_RATE,
            CASE 
                WHEN ROUND(AVG(s.SHIPPING_COST / 100), 2) &lt;= 0.5 THEN 'COST_EFFICIENT'
                WHEN ROUND(AVG(s.SHIPPING_COST / 100), 2) &lt;= 1.0 THEN 'MODERATE_COST'
                ELSE 'HIGH_COST'
            END as COST_EFFICIENCY
        FROM SHIPMENTS s
        WHERE s.SHIPMENT_STATUS = 'DELIVERED'
        AND s.CREATED_AT >= SYSDATE - 30
        GROUP BY s.SHIPPING_METHOD
        HAVING COUNT(*) &gt;= 5
        ORDER BY SHIPMENT_COUNT DESC, ON_TIME_RATE DESC
    </select>

</mapper>
